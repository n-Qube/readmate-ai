# Release record RM-20260925-1

Follows the ReadMate Production Launch Playbook v1.0 (24 Sep 2026), page 19.
Status labels: SOURCE (inspected code), REPORTED (described in PR or runbook),
VERIFIED RUN (retained exact-candidate output), LIVE VERIFIED (authorized
deployed-system check), PROPOSED (target). Secrets are listed by name and
version only.

## Inventory (captured 2026-09-25)

| Field | Value | Label |
|---|---|---|
| release_id | RM-20260925-1 | |
| source_sha | `9443a751f09d870c271caa349487f47ebd7375dd` (PR #1 head, branch `feat/gemini-tts-production-hardening`; open, mergeable, 36 commits ahead of `main` `2dfd47da`) | SOURCE |
| ci_run_url | Every `release-gates` run fails before any step executes: 3 s, no runner assigned, 0 steps. This is the GitHub Actions billing lock, not a test failure. **No hosted CI evidence exists for any candidate.** | LIVE VERIFIED |
| api | `readmate-api-build-cb191e008a903a1d`, 100% traffic, us-central1, image `readmate-api@sha256:c7d0a6336b0802c272f3fe8120f415d06031a6a4faecd3a1492e45f75d730888`, 1 vCPU / 2 GiB, built from `faf81c6` | LIVE VERIFIED |
| api rollback | `readmate-api-build-935cd4dcae8607b1` (same secrets, same schema) | REPORTED |
| migrations | 25 in repo; deploy pipeline (migration role): "No pending migrations to apply". The app role cannot read `_prisma_migrations`, so `prisma migrate status` under it lists every migration as pending; this is expected least privilege. | VERIFIED RUN |
| web | app.readmate.n-qube.com, Firebase site `readmate-ai-9df42`, exported from `9443a751`, artifact manifest sha256 `73b002014bf353fdd97acc99a1d4768154025d035b1865ada10c1976ba271648`, production guard mode, `Origin-Trial` header present | LIVE VERIFIED |
| ios | 1.0.2 (159), from `faf81c6c`; in TestFlight group "Internal Testers" (Ready to Submit). App Store version 1.0.2 is still attached to crashing build 146 and in "Unresolved Issues" (2.1(a) launch crash on builds 133/146). | LIVE VERIFIED |
| android | 1.0.2 (70), from `faf81c6c`; AAB built, **not uploaded**. Play upload is blocked by the overdue Foreground service declaration (needs video). | VERIFIED RUN |
| extension | manifest 0.1.6; not packaged or published in this release | SOURCE |
| secret_versions | All pinned to `:latest`, and each currently resolves to v1: supabase-service-role-key, clerk-secret-key, clerk-publishable-key, database-app-url, supabase-url, gemini-api-key, khaya-api-key, cron-secret, webmcp-audit-digest-key. **Gap (RM-07): pin explicit versions.** | LIVE VERIFIED |
| providers | Gemini 3.8 Flash TTS (Premium) / Flash-Lite TTS (all plans) with Google TTS fallback; key on the **free tier**, 429s observed, and fallback is logged as `tts_provider_fallback`. Khaya for Ga/Ewe, on-server Nano-Twi for Twi. Free plan cap 25,000 TTS characters per day. | LIVE VERIFIED |
| data_tests | Unit/route tests: API 350, mobile 196 (local, `faf81c6`/`9443a751`). **No real-PostgreSQL RLS/conversion/upload/deletion suite yet (RM-02/03/04).** | VERIFIED RUN (local only) |
| recovery | Not performed (RM-08). | — |
| alerts | Not configured or proven (RM-09). | — |
| devices | iPhone 17 / iOS 27.2: dev-signed Release build of `8d6c4b6` launched, no crash reports (1 launch). Simulator UAT (iOS 26.5, `fed098d`): onboarding, setup, Google sign-in, playback races, Twi/Ewe/Ga, study, add-URL passed. **Store binary 159: 0 device launches recorded; no iPad; no Android device.** | VERIFIED RUN (partial) |
| rollback | API: previous revision above. Web: previous Firebase release (keep). Mobile: previous TestFlight builds remain available. | REPORTED |

## Consequential actions taken 2026-09-24/25 (each individually approved in chat)

- API promotions to 100%: `083cac74` (Khaya streamed WAV), `ec7ee05f` (RSS artwork), `935cd4dc` (2 GiB memory, titles), `cb191e00` (saved-page titles, cloze flashcards). Each passed the authenticated smoke test (19-21 checks) at 0% traffic first.
- Data repair (owner account only, RLS-scoped, dry run first): 19 generated covers replaced, 228 truncated RSS titles restored (`src/jobs/backfillRssArtwork.ts`).
- Clerk production: password sign-up and "add password" turned off (password disabled, not required).
- IAM: `niinortey@n-qube.com` granted Firebase Hosting Admin on `readmate-ai-9df42`.
- Chrome origin trial: WebMCP registered for `https://app.readmate.n-qube.com` (expires 2026-11-17).
- Web deploy to production (guarded, production mode).
- Builds submitted by the owner: iOS 157, 158, 159 to TestFlight.

## Gate status

| Gate | Status | Evidence / gap |
|---|---|---|
| G0 / Source | **Not evidenced** | Clean non-iCloud clone and SHA recorded, but main is not the candidate (PR #1 unmerged) and hosted CI cannot run (billing lock). Mobile builds are from `faf81c6`, web from `9443a751`: the API/runtime diff between them is a backfill script only. |
| G1 / Data/security | **Partial** | **RM-02 Implemented, verified locally on real PostgreSQL 16 (not deployed, no hosted CI):** as `readmate_api` (no superuser, no BYPASSRLS; RLS forced on ReadingDocument/UploadedFile), 8 concurrent conversions produce 1 document (old path: **8**), a retry returns the canonical document, a foreign upload creates nothing, RLS hides other users' rows, and the deletion fence blocks creation. Run: `npm run test:pg -w @readmate/api` with `READMATE_PG_TEST_URL`/`READMATE_PG_ADMIN_URL` (all 25 migrations applied plus a minimal `storage.buckets` stub). RM-03 and RM-04 (full RLS/deletion suite): not started. |
| G2 / Validation | **Not evidenced** | Local tests only; no exact-main CI; no real-DB stage. |
| G3 / Deployment | **Partial** | Candidate then promotion used with smoke tests; migrations verified by migration role. Secrets not pinned; rollback target not re-tested. |
| G4 / Recovery | **Not evidenced** | No DB + file restore drill; no alerts. |
| G5 / Device pilot | **Not evidenced** | Store binaries 159/70 not yet tested on real devices (proposed: 20 cold launches per device, iPhone + iPad + lower-end Android). |
| G6 / Public release | **Not evidenced** | No App Review account; Play Foreground service declaration outstanding; Data safety review for Gemini/sharing pending. |

**Decision: HOLD.** No store submission for review and no broader distribution until the relevant gates pass. Internal TestFlight testing of 159 may continue.

## Owner actions required

1. Clear the GitHub Actions billing lock (G0/G2).
2. Name the release team: implementation engineer, independent data/security reviewer, device tester, release operator, backup contact.
3. Approve provider spend: Gemini paid tier or not; free and premium daily caps.
4. App Review sign-in account (a Google account for the reviewer); Play Foreground service video.

## Next engineering work (playbook order)

1. RM-02: idempotent PDF conversion with real-PostgreSQL failure-injection tests.
2. RM-03: actual-size upload enforcement.
3. RM-04: RLS and account-deletion suite as the API role.
4. RM-07: pin secret versions (needs approval: redeploy).
